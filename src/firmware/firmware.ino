
#include "explorer_motori.h"
#include "explorer_bumper.h"
#include "explorer_light.h"
#include "explorer_params.h"
#include "explorer_globals.h"
#include "explorer_sound.h"
#include <Arduino.h>



void setup() {
    /*
    Serial.begin(9600);
    Serial.println("Serial inizialized");
    Motori_init(&motori);
    Serial.println("Motori inizialized");
    for(int i=0;i<NUM_SENSOR_LIGHT;++i){
        Luci_init(&luci[i]);
    }
    Serial.println("Luci inizialized");
    Bumper_init(&bumper);
    Serial.println("Bumper inizialized");
    Gas_init(&gas);
    Serial.println("Gas inizialized");
    Suono_init(&suono);
    Serial.println("Suono inizialized");*/
}

void loop() {
}
